spring:
  cloud:
    config:
      enabled: false

eureka:
  client:
    enabled: false

client:
  application:
    url: http://localhost:${wiremock.server.port}
  balance:
    url: http://localhost:${wiremock.server.port}
  entry:
    url: http://localhost:${wiremock.server.port}